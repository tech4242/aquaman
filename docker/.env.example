# Aquaman Docker environment configuration
# Copy to docker/.env and fill in your values:
#   cp docker/.env.example docker/.env

# ── Credential backend ──────────────────────────────────────────────
# Options: encrypted-file (default in Docker), vault, 1password, keychain
AQUAMAN_BACKEND=encrypted-file

# ── Encrypted-file backend ──────────────────────────────────────────
# Required when AQUAMAN_BACKEND=encrypted-file
# AQUAMAN_ENCRYPTION_PASSWORD=your-strong-password-here
# Then seed credentials:
#   docker compose -f docker/docker-compose.yml run --rm aquaman credentials add anthropic api_key

# ── HashiCorp Vault backend ─────────────────────────────────────────
# AQUAMAN_BACKEND=vault
# VAULT_ADDR=https://vault.example.com
# VAULT_TOKEN=hvs.your-token-here
# VAULT_NAMESPACE=admin

# ── 1Password backend ───────────────────────────────────────────────
# AQUAMAN_BACKEND=1password
# OP_SERVICE_ACCOUNT_TOKEN=ops_your-token-here

# ── Proxy settings ──────────────────────────────────────────────────
# AQUAMAN_PORT=8081
# AQUAMAN_SERVICES=anthropic,openai,slack,discord,github
# AQUAMAN_TLS_ENABLED=false
# AQUAMAN_AUDIT_ENABLED=true

# ── Proxy client auth ────────────────────────────────────────────────
# Shared secret between aquaman proxy and OpenClaw plugin.
# When set, all proxy requests (except /_health) require X-Aquaman-Token header.
# Generate with: openssl rand -hex 32
# AQUAMAN_CLIENT_TOKEN=

# ── OpenClaw + Plugin (--profile with-openclaw) ──────────────────────
# Gateway auth token (required — set to a random string for production):
# OPENCLAW_GATEWAY_TOKEN=your-secret-token
# Set automatically in docker-compose.yml; override here if needed:
# AQUAMAN_PROXY_URL=http://aquaman:8081

# ── OpenClaw Sandbox (--profile with-openclaw-sandboxed) ─────────────
# Enables OpenClaw's built-in sandbox for tool execution isolation.
# Requires Docker socket access (/var/run/docker.sock).
# Tools execute in ephemeral containers on the aquaman-frontend network.
#
# Docker socket GID — set to the GID of the docker group on your host:
#   Linux:  DOCKER_GID=$(getent group docker | cut -d: -f3)
#   macOS:  DOCKER_GID=0  (Docker Desktop maps socket as root:root)
# DOCKER_GID=0
