# ──────────────────────────────────────────────────────────────
# Aquaman Docker Environment
# Copy to .env and fill in values for your chosen backend.
# ──────────────────────────────────────────────────────────────

# ── Backend Selection ─────────────────────────────────────────
# Which credential store to use.
# Options: keychain | encrypted-file | keepassxc | 1password | vault
# Default: encrypted-file
AQUAMAN_BACKEND=encrypted-file

# ── Services ──────────────────────────────────────────────────
# Comma-separated list of services to proxy credentials for.
# Default: anthropic,openai,slack,discord,github
# AQUAMAN_SERVICES=anthropic,openai,slack,discord,github

# ── Encrypted File Backend ────────────────────────────────────
# Required when AQUAMAN_BACKEND=encrypted-file
# Passphrase used to encrypt/decrypt the local credential store.
# AQUAMAN_ENCRYPTION_PASSWORD=

# ── KeePassXC Backend ─────────────────────────────────────────
# Required when AQUAMAN_BACKEND=keepassxc
# Master password for the .kdbx database file.
# AQUAMAN_KEEPASS_PASSWORD=

# ── HashiCorp Vault Backend ───────────────────────────────────
# Required when AQUAMAN_BACKEND=vault
# VAULT_ADDR=https://vault.example.com:8200
# VAULT_TOKEN=
# VAULT_NAMESPACE=                  # Optional — for Vault Enterprise namespaces

# ── 1Password Backend ─────────────────────────────────────────
# Requires the `op` CLI installed and signed in inside the container.
# No env vars needed — 1Password uses its own auth session.
# See: https://developer.1password.com/docs/cli/

# ── Audit Logging ─────────────────────────────────────────────
# Enable or disable the hash-chained audit log.
# Default: true
# AQUAMAN_AUDIT_ENABLED=true

# ── API Keys (for `aquaman setup --non-interactive`) ──────────
# Only needed if running setup inside the container.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
